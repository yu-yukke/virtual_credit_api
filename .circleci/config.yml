version: 2

workflows:
  version: 2
  workflows:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: circleci/ruby:2.7.5
    working_directory: ~/repo
    steps:
      - checkout
      - run: echo "hello world"
